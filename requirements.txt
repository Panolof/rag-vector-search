# Compatibility list for pip users. This unhashed file bypasses the pre-install
# gate. The security-reviewed path is `python3 scripts/verify_lock.py`, then
# `uv sync --locked`, which fixes and hashes every transitive dependency.
Flask==3.1.3
milvus-lite==3.1.0
pymilvus==3.0.1
