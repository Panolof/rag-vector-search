# Docker is no longer part of the v0.2 demo.
#
# Milvus Lite runs inside the Python process and persists to RAG_DB_URI. Keeping
# this valid, empty Compose file makes the migration explicit without retaining
# old images, default credentials, host port exposure, or disabled seccomp.
name: rag-vector-search
services: {}
